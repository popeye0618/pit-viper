# pit-viper 리포트

| | |
|---|---|
| 생성 시각 | 2026-08-16 14:06:01 KST |
| 프로젝트 | pit-viper |
| 브랜치 | main (f96ebae) |
| 대상 범위 | 전체 (com.pitviper.common.vo.* · customer.* · order.*) |
| 회전 | 3회 · 뮤턴트당 시도 예산 3회 |

## 요약

| 지표 | 전 | 후 |
|---|---|---|
| 뮤테이션 스코어 | 66.23% | **98.7%** |
| 잡힌 뮤턴트 | 51/77 | **76/77** |
| 구멍 (생존 + 무커버) | 26 | **1** |

기준선의 구멍 26개 중 **25개를 죽였다** (96%).

## 죽인 뮤턴트 (25)

| 위치 | 무엇을 바꿨나 | 시도 |
|---|---|---|
| Money#isGreaterThan:50 | 경계 조건을 바꿨다 (`<` ↔ `<=`, `>` ↔ `>=`) | 0 |
| Money#isZero:54 | 반환값을 항상 `true` 로 바꿨다 | 0 |
| Money#minus:34 | 경계 조건을 바꿨다 (`<` ↔ `<=`, `>` ↔ `>=`) | 0 |
| Money#minus:34 | 크기 비교를 없애고 거짓으로 고정했다 | 0 |
| Customer#isVip:26 | 조건을 뒤집었다 | 0 |
| Customer#isVip:26 | 같음 비교를 없애고 거짓으로 고정했다 | 0 |
| Customer#isVip:26 | 같음 비교를 없애고 거짓으로 고정했다 | 0 |
| Grade#isAtLeast:25 | 경계 조건을 바꿨다 (`<` ↔ `<=`, `>` ↔ `>=`) | 1 |
| Grade#isAtLeast:25 | 크기 비교를 없애고 거짓으로 고정했다 | 1 |
| DiscountPolicy#rateFor:32 | 크기 비교를 없애고 거짓으로 고정했다 | 1 |
| DiscountPolicy#rateFor:33 | 반환값을 0으로 바꿨다 | 1 |
| PointPolicy#accrue:25 | 조건을 뒤집었다 | 1 |
| PointPolicy#accrue:25 | 같음 비교를 없애고 거짓으로 고정했다 | 1 |
| PointPolicy#accrue:25 | 같음 비교를 없애고 거짓으로 고정했다 | 1 |
| PointPolicy#accrue:27 | 경계 조건을 바꿨다 (`<` ↔ `<=`, `>` ↔ `>=`) | 0 |
| PointPolicy#accrue:33 | 같음 비교를 없애고 거짓으로 고정했다 | 0 |
| PointPolicy#accrue:34 | 산술 연산자를 바꿨다 (`+` ↔ `-`, `*` ↔ `/`) | 0 |
| PointPolicy#canRedeem:45 | 경계 조건을 바꿨다 (`<` ↔ `<=`, `>` ↔ `>=`) | 0 |
| PointPolicy#canRedeem:45 | 크기 비교를 없애고 거짓으로 고정했다 | 0 |
| PointPolicy#canRedeem:48 | 경계 조건을 바꿨다 (`<` ↔ `<=`, `>` ↔ `>=`) | 0 |
| ShippingCalculator#isFreeShipping:30 | 반환값을 항상 `true` 로 바꿨다 | 0 |
| OrderService#finalPrice:33 | 경계 조건을 바꿨다 (`<` ↔ `<=`, `>` ↔ `>=`) | 1 |
| OrderService#finalPrice:33 | 크기 비교를 없애고 거짓으로 고정했다 | 1 |
| OrderService#finalPrice:36 | 경계 조건을 바꿨다 (`<` ↔ `<=`, `>` ↔ `>=`) | 1 |
| OrderService#finalPrice:36 | 크기 비교를 없애고 거짓으로 고정했다 | 1 |

## 동등 뮤턴트로 닫은 것 (1)

| 위치 | 무엇을 바꿨나 | 동등으로 판정한 이유 |
|---|---|---|
| DiscountPolicy#rateFor:32 | 경계 조건을 바꿨다 (`<` ↔ `<=`, `>` ↔ `>=`) | rate 는 (큰 주문 0.10) + (등급 보너스 0/0.05/0.10/0.20) 의 합이라 도달 가능한 값이 {0, 0.05, 0.1, 0.15, 0.2, 0.3} 뿐이다. MAX_RATE(0.25) 와 같아지는 입력이 없어 > 와 >= 를 가르는 관측이 불가능하다. |

