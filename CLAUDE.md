# pit-viper — 작업 지침

> **프로젝트**: 브랜치 diff의 변경 클래스만 Jacoco + pitest로 스캔해 테스트의 구멍(미커버 코드, 생존 뮤턴트)을 찾고, Claude Code 에이전트가 그 구멍을 메우는 테스트를 쓰고, 스크립트가 재실행 결과로 채점하는 **Claude Code 스킬**.
>
> 제품은 jar가 아니라 `.claude/skills/pit-viper/`(SKILL.md + 결정적 스크립트) 디렉터리다.
> 저장소 루트는 그 스킬을 돌릴 무대인 **스프링 프로젝트**다 — 소비자 프로젝트의 모습을 그대로 보여주는 것이 데모다.

**목적은 도구 제품이 아니라 AI 에이전트를 개발 과정에 포함시키는 것이다.** 한때 독립 Java 도구로 만들려 했으나(2026-08-16 피벗), AI 배관(컨텍스트 수집·프롬프트 렌더링·AI 호출)은 Claude Code가 이미 제공하므로 다시 만들지 않는다.

---

## 스텝 진행 순서 (항상 이 순서를 지킨다)

프로젝트는 **Step 단위**로 진행한다. 각 스텝은 기능 하나다. 모든 스텝을 아래 5단계로 진행한다.

### 1. 이번 스텝에서 만드는 기능을 먼저 밝힌다

코드를 쓰기 전에 한 문장으로 선언한다. "이번엔 이 기능을 만드는구나"가 사용자에게 먼저 전달되어야 한다.
왜 지금 이 순서인지(앞 스텝과의 의존 관계)도 함께 짚는다.

### 2. 코드를 작성한다

**Claude가 코드를 전부 작성한다.** 사용자에게 구현을 넘기지 않고, "직접 써보시겠어요?" 같은 제안도 하지 않는다.
스크립트·SKILL.md·프로젝트 코드 모두 "코드"다 — 같은 기준을 적용한다.

### 3. 무엇을 왜 그렇게 했는지 설명한다

결과만 던지지 않는다. 코드를 제공한 뒤 반드시 설명한다:
- 이 파일/스크립트가 무슨 역할인지
- 왜 그렇게 나눴는지 (설계 의도)
- 어떤 대안을 버렸는지, 그 이유는 무엇인지
- 처음 보는 문법·패턴이 있으면 그것도

### 4. 게이트를 실제로 돌려 통과를 확인한다

계획 문서의 🚦 게이트를 **실제로 실행해서** 확인한다.
"잘 된 것 같다"는 통과가 아니다. 명령을 돌리고 출력을 보여준다.
게이트가 막히면 다음 스텝으로 넘어가지 않는다. 막힌 이유를 먼저 해결한다.

### 5. 체크박스를 갱신하고 증거를 기록한다

- 옵시디언 계획 문서의 작업 체크박스와 게이트 체크박스를 채운다
- `00. 로드맵.md`의 **현재 위치**를 다음 스텝으로 옮긴다
- README의 진행 상황 체크박스도 함께 갱신한다
- **`진행 기록.md`에 증거를 원문 그대로 남긴다** — 실행한 명령과 그 출력을 요약하지 말고 그대로 붙인다.
  게이트 판정 표, 그리고 그 스텝에서 내린 결정·발견도 함께 적는다.
  나중에 "그때 정말 통과했나?"를 다시 물을 때 이 문서가 답한다.

---

## 설명 수준

사용자는 **Java 기본기는 있으나 실무 경험이 적다.** 이 수준에 맞춰 설명한다.

- Python 스크립트가 주 재료가 됐다 — `argparse`, `pathlib`, `ElementTree` 같은 관용구는 처음 쓸 때 설명한다
- 스킬(SKILL.md)이 어떻게 로드되고 동작하는지, 에이전트 루프 설계는 도입할 때 설명한다
- 테스트 패턴(뮤테이션 킬 전략, 경계값 테스트 등)은 도입할 때 설명한다
- Gradle 설정은 "이 줄이 무슨 일을 하는가"를 짚는다

**목표는 진도가 아니라 사용자가 코드를 읽고 손댈 수 있는 상태를 유지하는 것이다.**

---

## 계획 문서

계획은 옵시디언 보관함에 있다: `~/Documents/Obsidian/Vault/pit-viper/`

| 문서 | 내용 |
|---|---|
| `00. 로드맵.md` | 전체 지도, 현재 위치, 진행 규칙, 스텝별 게이트 요약 |
| `Phase A - 채점 기계.md` | S1–S3 (결정적 스크립트, AI 없음) |
| `Phase B - 에이전트 루프.md` | S4–S5 (SKILL.md, 수렴 루프) |
| `Phase C - 실전 이식.md` | S6 (다른 프로젝트로 이식) |
| `진행 기록.md` | 완료 증거 원문 |
| `archive - Java 도구/` | 피벗 전 계획 문서. 참고용 |

---

## 설계 원칙 (모든 결정의 심사 기준)

1. **AI를 채점자 없는 자리에 두지 않는다.** 에이전트의 입력은 스크립트가 뽑은 목표(뮤턴트 좌표·미커버 라인)여야 하고, 출력은 기계가 채점(컴파일·테스트·pitest 재실행)할 수 있어야 한다. **에이전트는 자기 채점을 하지 않는다 — 킬 판정은 스크립트 출력만 신뢰한다.**
2. **결정적 도구가 할 수 있는 일은 AI에게 시키지 않는다.** 파싱·선별·채점·리포트 조립은 전부 `scripts/`의 일반 코드다. AI는 "테스트 코드 작성"과 "equivalent 판정" 두 지점에만 존재한다.
3. **프롬프트는 코드다.** SKILL.md와 스크립트는 git으로 관리하고, 변경하면 이 프로젝트에 다시 돌려 킬률 게이트가 유지되는지 확인한다. **이 프로젝트가 스킬의 회귀 테스트다.**
4. **루프는 수렴해야 한다.** 시도 예산은 지침이 아니라 상태 파일을 관리하는 스크립트가 강제한다. 예산을 넘긴 뮤턴트는 목표 목록에서 기계적으로 빠지므로, 에이전트가 예산을 "잊어도" 무한 재시도가 불가능하다.
5. **에이전트는 `src/main`을 만지지 않는다.** 테스트를 통과시키려 프로덕션 코드를 고치는 것은 실패 축에 속한다. guard 스크립트가 diff를 검사해 위반 시 실패시킨다.

---

## 기준선 (S0에서 고정 — 스킬 개발 중에는 `src/main`을 건드리지 않는다)

```
뮤테이션   총 78 · KILLED 52 (67%) · SURVIVED 24 · NO_COVERAGE 2  → 구멍 26개
커버리지   LINE 69/90 (76%) · BRANCH 28/44 (63%)
```

**이 숫자가 스킬의 회귀 테스트다.** SKILL.md나 스크립트를 고치면 다시 측정해 킬률이 떨어지지 않았는지 확인한다.
`src/main`을 수정하면 기준선이 무효가 되고 원칙 5도 깨진다 — 프로덕션 코드 변경이 필요하다고 판단되면 **먼저 사용자에게 알린다.**

---

## 코드 컨벤션

- 주석·docstring은 **한국어**로 쓴다
- 주석은 "무엇을"이 아니라 **"왜"**를 적는다. 코드를 읽으면 아는 것은 쓰지 않는다
- **프로젝트 코드에 "여기 구멍을 심었다" 같은 주석을 쓰지 않는다.** 평범한 프로젝트로 읽혀야 데모가 연출이 아니라 실제가 된다
- 스크립트는 **Python 3 표준 라이브러리만** 쓴다 — clone 직후 의존성 설치 없이 실행돼야 한다
- **의존성 버전은 실제 레지스트리에서 확인한다** — 기획서에 적힌 버전을 그대로 믿지 않는다 (첫 시도에서 여러 건 어긋났다)
- 커밋 메시지는 한국어. 무엇을 왜 했는지 본문에 적는다

---

## 저장소

`main` 하나만 쓴다. 이력은 2026-08-16에 새로 시작했다 (Java 도구 시절 이력은 폐기).

## 알려진 차단 사항

**GitHub Actions가 계정 결제 문제로 잠겨 있다** — 하지만 치명적이지 않다.
스킬은 사용자의 로컬 개발 루프에서 돌기 때문에 CI가 필수가 아니다.
나중에 CI 확장(선택 사항)을 원할 때만 GitHub Settings → Billing에서 해소하면 된다.
